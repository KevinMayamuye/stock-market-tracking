
  # Stock Updates App Design

  This is a code bundle for Stock Updates App Design. The original project is available at https://www.figma.com/design/ueG75b40zLzf6J3FVMrstN/Stock-Updates-App-Design.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  